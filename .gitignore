node_modules/
dist/
build/
.DS_Store
*.log
.env
.vscode/
.idea/
# Keep the canonical layout.json (ships as the demo) and .gitkeep tracked;
# ignore only the timestamped history snapshots that the server writes.
server/layouts/layout-*.json
