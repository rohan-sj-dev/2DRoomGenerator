# docs/ — screenshots

Drop the four images here, with these exact filenames so the main `README.md` picks them up:

| Filename                          | What to capture |
| --------------------------------- | --------------- |
| `demo-layout.png`                 | The sample layout that auto-loads on first launch — sofa, rug, armchairs, coffee table, dining table, TV, lamp, plant, bookshelf. |
| `layering-demo.png`               | A rug placed under a sofa (or vice-versa) showing explicit z-ordering — use **Bring to Front** / **Send to Back** to demonstrate. |
| `resizing-items.png`              | An item selected (dashed outline + white corner handles) mid-resize; include the live `Selected: type (x, y) W×H` readout. |
| `alignment-functionality.png`     | A drag in progress with one or more blue 1-px alignment guides lit, lining up edges with another piece. |

PNG is preferred. ~1280×800 viewport keeps the sidebar + room both readable.
