{
  "name": "2d-room-generator",
  "version": "1.0.0",
  "private": true,
  "description": "A 2D Room Generator - drag, drop, and resize furniture in a virtual room. Save & reload layouts.",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "setup": "npm run install:all",
    "start": "concurrently -k -n SERVER,CLIENT -c blue,green \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "dev": "npm start",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
